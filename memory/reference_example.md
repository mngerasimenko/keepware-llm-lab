---
name: reference_example
description: "Куда смотреть: дашборд Grafana (метрики) и внутренняя страница статуса инцидентов"
metadata:
  type: reference
---

Наблюдаемость демо-проекта (вымышленные адреса-заглушки):

- Дашборд метрик - Grafana, борд `demo-api / overview` (латентность, ошибки, RPS).
- Статус инцидентов - внутренняя страница `status.example.internal`.

Указатели живут в памяти отдельно от фактов: reference-файл говорит, *где* смотреть, а не дублирует содержимое.

Связано: [[project_example]].
