# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDE
.idea/
.vscode/
*.swp
*~

# Python
__pycache__/
*.pyc

# Слепок мутационного прогона (живёт только пока прогон идёт)
.mutation-backup
# Временный файл атомарной записи. В норме его нет: он живёт микросекунды
# между созданием и подменой. Остаётся, если прогон убили ровно в это окно.
*.tmp
